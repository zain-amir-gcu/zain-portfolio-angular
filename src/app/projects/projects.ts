import { Component } from '@angular/core';

@Component({
  selector: 'app-projects',
  templateUrl: './projects.html',
  styleUrls: ['./projects.css']
})
export class Projects {
  // Future enhancement: You could store projects as an array and *ngFor it
}
